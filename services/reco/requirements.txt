fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.0
# numpy MAINTENU EN 1.x : paddlepaddle 2.6 / paddleocr 2.7 sont compilés contre l'ABI numpy 1
# (numpy 2.x → « module compiled against ABI version 0x1000009 », échec de build constaté).
numpy>=1.26,<2
opencv-python-headless>=4.9
ultralytics>=8.2
fast-alpr>=0.1.0
onnxruntime>=1.17
# OCR-DOC (attestations, bilingue fr/ar) — bornes épinglées : l'API de PaddleOCR 3.x change, et
# 2.8+ tire albumentations→stringzilla (wheel absente → compilation requise, échec constaté en slim).
paddleocr>=2.7,<2.8
paddlepaddle>=2.6,<3.0
