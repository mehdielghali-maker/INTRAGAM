# Configuration de la PWA CLIENTE « Souscription auto » (face distante).
# Copier en .env.local et ajuster. Aucune valeur sensible n'est commitée.

# Mode de l'adaptateur : mock (dev, par défaut) ou real (backend GAM /SecGam).
VITE_SOUSCRIPTION_MODE=mock

# Backend GAM (utilisé seulement en mode real).
VITE_SOUSCRIPTION_BASE_URL=https://api2.gam.dz/APIS/api/v2/
# [DSI à confirmer] nom + valeur de l'en-tête de clé d'API.
VITE_SOUSCRIPTION_API_KEY=
VITE_SOUSCRIPTION_API_KEY_HEADER=X-Api-Key

# Durée de session client (jours) — usage hors-ligne.
VITE_SESSION_AGENT_JOURS=7

# Préfixe de référence (format SCR-XXXX-AAAA).
VITE_CODE_PREFIXE=SCR
